package pages

import "goapp/web/layouts"

templ SettingsPage(title string) {
	@layouts.Base(title) {
		<div class="p-6">
			<h1 class="text-2xl font-bold mb-4">{ title }</h1>
			<p class="text-gray-500">Settings module — add your UI here.</p>
		</div>
	}
}
